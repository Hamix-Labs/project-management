.env
.env.local
.git-commit-msg.tmp
coverage.out
coverage*.out
coverage
coverage-handler
coverage-pg
node_modules/
web/dist/
cmd/hamix-desktop/build/
cmd/hamix-desktop/frontend/dist/assets/
taskapi-dev
taskapi-dev.exe
hamix-desktop-dev
hamix-desktop-dev.exe
/hamix-agent-mcp
/hamix-agent-mcp.exe
/hamix-draft-mcp
/hamix-draft-mcp.exe
/hamix-draft-agent
/hamix-draft-agent.exe
/hamix-draft-agent.cmd
/hamix-draft-agent.js
sidecars/hamix-draft-agent/dist/
sidecars/hamix-draft-agent/node_modules/
/worker
/cover-agents
cover-agents.out
/handler
/handler.exe
logs/
web/.env.local
web/.env.*.local
web/public/mock-data/graphs/*.json
work_technique/*.md
.agent/
notes/
.cursor/skills/
moat.md
codegraph-tools.md
possible_improvements.md

# Defense in depth against the 2026-04-19 incident: a Windows component
# in the cursor-agent child process tree (Software Licensing Service /
# ETW / .NET CLR / Defender) called ExpandEnvironmentStrings on a
# hardcoded path like "%SystemDrive%\ProgramData\..." against an env
# block missing SYSTEMDRIVE, got the literal "%SystemDrive%\..." string
# back, and CreateFile resolved it as a relative path under the child's
# cwd (the repo root). The root cause is fixed in
# pkgs/agents/runner/cursor/config.go::defaultPassthroughEnvKeys, but
# this pattern keeps `git status` clean even if a future Windows
# subsystem caches via an env var we forgot to forward.
%*%/

# Local UI mock / design reference — do not vendor
execution-cycles-interface/
agent-configuration-interface/

