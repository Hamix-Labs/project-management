# pkgs/runners

HTTP-only surface for the SPA runner registry (`/runners*`).

**Adapters and the `Runner` interface live in [`pkgs/agents/runner`](../agents/runner/)** — do not add CLI adapters here. See [ADR-0052](../../docs/adr/ADR-0052-runners-http-handler.md) and [handler/README.md](./handler/README.md).
