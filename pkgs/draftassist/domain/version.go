package domain

// DraftAssistSchemaVersion is the SSE session-event schema version.
// The SPA asserts this on the first session frame before treating the
// stream as trusted.
const DraftAssistSchemaVersion = 1
