/** Max active (non-done) tasks retained for the board cache. */
export const BOARD_ACTIVE_CAP = 500;

/** Max `GET /tasks` pages scanned during the active-task walk. */
export const BOARD_MAX_PAGES = 10;

/** Page size for each walk step (API max). */
export const BOARD_PAGE_SIZE = 200;
